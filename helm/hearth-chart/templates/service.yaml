apiVersion: v1
kind: Service
metadata:
  name: {{ include "hearth-chart.fullname" . }}
  labels:
    {{- include "hearth-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hearth-chart.selectorLabels" . | nindent 4 }}
